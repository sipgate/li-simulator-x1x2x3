spring:
  application:
    name: simulator
sipgate:
  li:
    simulator:
      targetUri: https://localhost/X1/NE
      admfIdentifier: admf-id
      clientCertKeyStore:
        path: "/app/keystore.p12"
        password: changeit
      serverCertTrustStore:
        path: "/app/truststore.jks"
        password: changeit

management:
  endpoints:
    web:
      base-path: /
